# Analogs Module

Foundation-only backend boundary for analog rule lookup and analog candidate decisions.

This module currently contains DTO placeholders and service interfaces only. It does not implement business logic, persistence, external calls, matching algorithms, pricing, email sending, or API routes.
