# Pricing Module

Foundation-only backend boundary for price source references and cart item price snapshots.

This module currently contains DTO placeholders and service interfaces only. It does not implement business logic, persistence, external calls, pricing algorithms, catalog identity mutation, email sending, or API routes.
