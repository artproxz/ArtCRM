# Matcher Module

Foundation-only backend boundary for deterministic catalog matching decisions.

Catalog Matcher is a backend service boundary, not an LLM agent. This module currently contains DTO placeholders and service interfaces only. It does not implement matching algorithms, persistence, external calls, parser/import logic, pricing, email sending, or API routes.
