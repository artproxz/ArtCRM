# Stock Module

Foundation-only backend boundary for stock snapshot references and future stock lookup.

This module currently contains DTO placeholders and service interfaces only. It does not implement business logic, persistence, external calls, stock import, catalog identity creation, email sending, or API routes.
